
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weatherdashboard-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: weather.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: weatherdashboard-service
            port:
              number: 80
